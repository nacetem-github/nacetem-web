# App Assets

Use this folder for images and graphics that are imported by React/TypeScript code.

These files are bundled by Vite, fingerprinted during build, and referenced through imports such as `src/assets.ts`.

Use `public/uploads` instead for files that site editors upload and link to directly, such as brochures, seminar presentations, registration forms, and videos.
