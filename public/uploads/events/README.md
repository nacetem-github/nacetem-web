# Event Uploads

Use these folders for event files:

- `images/`
- `gallery/<event-slug>/`
- `registration-forms/`
- `videos/`

Gallery folders should use event-style slugs that match the entries in `src/pages/News.tsx`.
